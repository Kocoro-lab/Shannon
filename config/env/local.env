# ============================================================================
# Shannon Local Mode Configuration
# ============================================================================
# For standalone desktop operation without external services.
# No Docker, Redis, PostgreSQL, or Temporal required.
#
# Usage:
#   source config/env/local.env
#   cargo run -p shannon-api --no-default-features --features "embedded,gateway"
#
# Or use the convenience script:
#   ./scripts/run-local.sh
# ============================================================================

# -----------------------------------------------------------------------------
# DEPLOYMENT MODE
# -----------------------------------------------------------------------------
# Mode: embedded = Self-contained desktop app with local storage
SHANNON_MODE=embedded

# Workflow Engine: durable = Local in-process workflow execution
WORKFLOW_ENGINE=durable

# Database: surrealdb = Embedded multi-model database
DATABASE_DRIVER=surrealdb

# Path to store the SurrealDB database file
SURREALDB_PATH=./data/shannon.db

# -----------------------------------------------------------------------------
# SERVER SETTINGS
# -----------------------------------------------------------------------------
# Bind to localhost only for local mode (security)
SHANNON_HOST=127.0.0.1

# Port for the API server
SHANNON_PORT=8765

# Request timeout in seconds
SHANNON_TIMEOUT_SECS=300

# -----------------------------------------------------------------------------
# LLM PROVIDER API KEYS
# -----------------------------------------------------------------------------
# At least one API key is required. Uncomment and set the ones you want to use.

# Anthropic Claude (recommended for best quality)
ANTHROPIC_API_KEY=

# OpenAI GPT models
# OPENAI_API_KEY=

# Google Gemini
# GOOGLE_API_KEY=

# Groq (fast inference)
# GROQ_API_KEY=

# xAI Grok
# XAI_API_KEY=

# -----------------------------------------------------------------------------
# SYNC (DISABLED FOR LOCAL-ONLY)
# -----------------------------------------------------------------------------
SYNC_MODE=disabled

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
RUST_LOG=info,shannon_api=debug

# Disable OpenTelemetry for local mode
OTEL_EXPORTER_OTLP_ENDPOINT=
