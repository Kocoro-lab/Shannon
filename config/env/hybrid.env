# ============================================================================
# Shannon Hybrid Mode Configuration
# ============================================================================
# Local-first operation with optional cloud sync.
# Data is stored locally but can sync to the cloud for backup/cross-device.
#
# Usage:
#   source config/env/hybrid.env
#   cargo run -p shannon-api --no-default-features --features "embedded,gateway"
# ============================================================================

# -----------------------------------------------------------------------------
# DEPLOYMENT MODE
# -----------------------------------------------------------------------------
# Mode: hybrid = Local-first with cloud sync capability
SHANNON_MODE=hybrid

# Workflow Engine: durable = Local in-process workflow execution
WORKFLOW_ENGINE=durable

# Database: surrealdb = Embedded multi-model database
DATABASE_DRIVER=surrealdb

# Path to store the SurrealDB database file
SURREALDB_PATH=./data/shannon.db

# -----------------------------------------------------------------------------
# SERVER SETTINGS
# -----------------------------------------------------------------------------
# Bind to localhost for security
SHANNON_HOST=127.0.0.1

# Port for the API server
SHANNON_PORT=8765

# Request timeout in seconds
SHANNON_TIMEOUT_SECS=300

# -----------------------------------------------------------------------------
# CLOUD SYNC CONFIGURATION
# -----------------------------------------------------------------------------
# Sync mode: cloud-only = Sync to central cloud server only (no P2P)
SYNC_MODE=cloud-only

# Cloud sync endpoint
SYNC_CLOUD_ENDPOINT=https://api.shannon.io/sync

# Cloud sync API key (obtain from your Shannon cloud account)
SYNC_CLOUD_API_KEY=

# Sync interval in seconds
SYNC_INTERVAL_SECS=30

# -----------------------------------------------------------------------------
# LLM PROVIDER API KEYS
# -----------------------------------------------------------------------------
# At least one API key is required

# Anthropic Claude (recommended)
ANTHROPIC_API_KEY=

# OpenAI GPT
# OPENAI_API_KEY=

# Google Gemini
# GOOGLE_API_KEY=

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
RUST_LOG=info,shannon_api=debug

# Disable OpenTelemetry for local mode
OTEL_EXPORTER_OTLP_ENDPOINT=
