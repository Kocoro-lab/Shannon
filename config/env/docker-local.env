# ============================================================================
# Shannon Docker Local Mode Configuration
# ============================================================================
# For testing embedded mode in Docker containers.
# Uses embedded database and workflow engine, but runs in a container.
#
# Usage:
#   docker compose -f deploy/compose/docker-compose.yml --profile local up
# ============================================================================

# -----------------------------------------------------------------------------
# DEPLOYMENT MODE
# -----------------------------------------------------------------------------
# Mode: embedded = Self-contained with local storage
SHANNON_MODE=embedded

# Workflow Engine: durable = Local in-process workflow execution
WORKFLOW_ENGINE=durable

# Database: surrealdb = Embedded multi-model database
DATABASE_DRIVER=surrealdb

# Path inside the container for database storage
# This should be mounted as a volume for persistence
SURREALDB_PATH=/data/shannon.db

# -----------------------------------------------------------------------------
# SERVER SETTINGS
# -----------------------------------------------------------------------------
# Bind to all interfaces for container networking
SHANNON_HOST=0.0.0.0

# Port for the API server
SHANNON_PORT=8080

# Request timeout in seconds
SHANNON_TIMEOUT_SECS=300

# -----------------------------------------------------------------------------
# LLM PROVIDER API KEYS
# -----------------------------------------------------------------------------
# At least one API key is required

# Anthropic Claude
ANTHROPIC_API_KEY=

# OpenAI GPT
# OPENAI_API_KEY=

# -----------------------------------------------------------------------------
# SYNC (DISABLED)
# -----------------------------------------------------------------------------
SYNC_MODE=disabled

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
RUST_LOG=info,shannon_api=debug

# Disable OpenTelemetry for local mode
OTEL_EXPORTER_OTLP_ENDPOINT=
